// Description:
//    Vérification de la précédence des opérateurs et associativité.
//  
//
// Resultats:
//    Syntaxiquement correct.

{
    boolean b;
    float x;
    
    // Priorité ET vs OU
    // Doit être : (a && b) || c
    int a;
    b = true && false || true;
    x = -3 * 5;
    b = ! (int) (a.b);
    if (true) {
        if (false) {

            println("Interne");
        }
        else {
            println("Else Interne");
        }
    }
}

    