{
    // Déclarations
    int i = 5;
    float f = 2.5;
    float res;

    // Instructions
    // Test: int + float -> float (Conversion implicite)
    res = i + f; 
    
    // Test: Priorité des opérations et parenthèses
    res = (i * 2) + f;
    
    println(res);
}